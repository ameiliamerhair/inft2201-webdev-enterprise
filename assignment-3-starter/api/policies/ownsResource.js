// For the mail API, the resource will be req.mail.
// Returns true if mail.userId === user.userId.

module.exports = (user, mail) => user.userId === mail.userID; {
  // TODO: implement
  return false;
};